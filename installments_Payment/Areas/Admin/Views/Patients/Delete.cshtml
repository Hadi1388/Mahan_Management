@model installments_Payment.DataAccessLayer.Entities.Users.Patient

@{
    ViewData["Title"] = "حذف بیمار";
}

<div class="container">
    <div class="card shadow-sm">
        <div class="card-body">
            <h3>اَز حذف بیمار <span class="text-danger">'@Model.FullName'</span> مطمئن هستید؟</h3>
            <hr class="p-2 mt-2" />
            <form asp-action="Delete">
                <div class="d-flex justify-content-end">
                    <input type="hidden" asp-for="Id" />
                    <input type="submit" value="بله" class="btn-gradient-red mx-1" />
                    <a class="btn-gradient-gray" asp-action="Index">خیر</a>
                </div>
            </form>
        </div>
    </div>
</div>
